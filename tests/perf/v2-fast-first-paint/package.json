{
  "name": "@superdoc-testing/v2-perf-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Benchmark harness for v2 fast-first-paint pipeline.",
  "scripts": {
    "bench": "playwright test --config playwright.config.ts",
    "bench:all": "pnpm run bench:model && pnpm run bench:browser",
    "bench:browser": "playwright test --config playwright.config.ts",
    "bench:model": "vitest run --config vitest.config.ts",
    "bench:watch": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    "@superdoc/v2-model": "workspace:*",
    "@superdoc/v2-perf": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
